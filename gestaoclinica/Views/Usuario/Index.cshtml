@using (Html.BeginForm("Index", "Usuario", FormMethod.Get))
{
    if (TempData["MsgSucesso"] != null)
    {
        <div class="alert alert-success">
            <a href="#" class="close" data-dismiss="alert">×</a>
            @TempData["MsgSucesso"]
        </div>
    }

    if (TempData["MsgErro"] != null)
    {
        <div class="alert alert-danger">
            <a href="#" class="close" data-dismiss="alert">×</a>
            @TempData["MsgErro"]
        </div>
    }

    <nav aria-label="breadcrumb">
        <ol class="breadcrumb small">
            <li class="breadcrumb-item"><a href="#">Cadastros</a></li>
            <li class="breadcrumb-item" aria-current="page">@Html.ActionLink("Usuário", "Index", "Usuario")</li>
            <li class="breadcrumb-item active"><a href="#">Gerenciamento</a></li>
        </ol>
    </nav>


    <button type="button" class="btn btn-primary btn-sm" onclick="location.href='@Url.Action("Cadastro", "Usuario")'"><i class="fas fa-plus"></i>&nbsp;Novo Cadastro</button>
    <button type="button" class="btn btn-secondary btn-sm"><i class="fas fa-file"></i>&nbsp;Relatório</button>

    <br /> <br />

    <div class="row">
        <div class="col-12">
            <div class="input-group">
                <div class="input-group-prepend">
                    <span class="input-group-text" id="basic-addon1"><i class="fas fa-search"></i></span>
                </div>
                <input type="text" id="pesquisa" name="pesquisa" class="form-control" placeholder="Digite um nome para pesquisar">
                <div class="input-group-append">
                    <button type="submit" class="btn btn-secondary btn-sm">Pesquisar</button>
                </div>
            </div>
        </div>
    </div>

    <br />

    <div class="table-responsive table-sm">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th scope="col"></th>
                    <th scope="col">#</th>
                    <th scope="col">Nome</th>
                    <th scope="col">Email</th>
                    <th scope="col">Celular</th>
                    <th scope="col">Status</th>
                </tr>
            </thead>
            <tbody>
                @{
                    foreach (gestaoclinica.Models.Usuario Usuario in ViewBag.Usuarios)
                    {
                        <tr>
                            <td>
                                <div class="btn-group btn-group-sm" role="group">
                                    <button type="button" class="btn btn-outline-info" title="Ver Detalhes" onclick="location.href='@Url.Action("Detalhe", "Usuario", new { Usuario.Codigo })'"><i class="fas fa-file"></i></button>
                                    <button type="button" class="btn btn-outline-primary" title="Editar" onclick="location.href='@Url.Action("Edicao", "Usuario", new { Usuario.Codigo })'"><i class="fas fa-edit"></i></button>
                                </div>
                            </td>
                            <th scope="row">@Usuario.Codigo.ToString()</th>
                            <td>@Usuario.Nome</td>
                            <td><i class="fas fa-envelope"></i>&nbsp;@Usuario.Email</td>
                            <td><i class="fas fa-phone"></i>&nbsp;@Usuario.Celular</td>
                            <td>@Usuario.Status</td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>

}





